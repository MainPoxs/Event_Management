<Page x:Class="EventManagement.Pages.ListOrganizersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EventManagement.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Список организаторов">
    <Grid DataContext="{DynamicResource eventManagement}">     
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>            
        </Grid>
        <DataGrid x:Name="dGr"
               ItemsSource="{Binding Users}" SelectedItem="{Binding User}"
               IsReadOnly="True" AutoGenerateColumns="False"
               SelectionMode="Single" CanUserSortColumns="False" Background="White">
            <DataGrid.Columns>                
                <DataGridTextColumn Header="Имя" Binding="{Binding Path=FirstName}" Width="*" MaxWidth="120"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Path=LastName}" Width="*" MaxWidth="120"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Path=Login}" Width="*" MaxWidth="120"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Path=Password}" Width="*" MaxWidth="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"
                    VerticalAlignment="Bottom">
            <Button Width="100" Content="Удалить"
                    Command="{Binding DeleteUser}" CommandParameter="{Binding User}"/>
            <Button Width="100" Content="Редактировать"                    
            Click="Button_EditUser"/>
            <Button Width="100" Content="Обновить" Command="{Binding LoadUser}"/>
        </StackPanel>
    </Grid>
</Page>
