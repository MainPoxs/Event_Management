<Page x:Class="EventManagement.Pages.StatisticPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EventManagement.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Отчет">
    <Grid DataContext="{DynamicResource eventManagement}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>          
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>            
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Мероприятие" Style="{StaticResource MainTextBlock}"/>
            <!--<ComboBox Grid.Row="1" ItemsSource="{Binding Events}" SelectedItem="{Binding Event_}"
                      DisplayMemberPath="EventName" SelectionChanged="{Binding SelectionEvent}"/>-->           
        </Grid>
        <DataGrid ItemsSource="{Binding Events}" SelectedItem="{Binding Event_}" Grid.Row="3" 
                  IsReadOnly="True" AutoGenerateColumns="False" SelectionMode="Single"
                  CanUserSortColumns="False" Margin="0,50">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Мероприятие" Binding="{Binding Path=EventName}"/>                 
                <DataGridTextColumn Header="Посетитель" Binding="{Binding Path=User.FullName}"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding Path=EventComment}"/>
            </DataGrid.Columns>
        </DataGrid>        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Grid.Row="4" Grid.Column="1" Width="200" Content="Сформировать отчет"
           Command="{Binding SaveFileStatistic}"/>
        </StackPanel>
    </Grid>
</Page>
