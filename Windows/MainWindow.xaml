<Window x:Class="EventManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EventManagement"
        mc:Ignorable="d"
        Title="Мероприятие" Height="520" Width="800" MinHeight="520" MinWidth="800">
    <Grid DataContext="{DynamicResource eventManagement}">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid Background="#ECECEC">
            <Image Source="/Resources/logo.png" HorizontalAlignment="Left"/>
            <TextBlock x:Name="HeaderTextBlock" Text="Мероприятия" HorizontalAlignment="Center"
                      VerticalAlignment="Center" FontSize="18" FontWeight="Bold"/>
            <Button x:Name="BackButton" Content="Назад" HorizontalAlignment="Right" VerticalAlignment="Center" Width="100"
                    Visibility="Collapsed" Click="BackButton_Click"/>
        </Grid>
        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden" Navigated="MainFrame_Navigated" />
        <Grid Grid.Row="2" Background="#ECECEC"/>
    </Grid>
</Window>
